<?xml version="1.0" encoding="utf-8"?>
<!-- Error variant of bg_input_field: swap an input's background to this when
     validation fails, pair with helper/error text below the field. -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/surface_input" />
            <corners android:radius="@dimen/radius_small" />
            <stroke
                android:width="1.5dp"
                android:color="@color/status_error" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/surface_input" />
            <corners android:radius="@dimen/radius_small" />
            <stroke
                android:width="1.5dp"
                android:color="@color/status_error" />
        </shape>
    </item>
</selector>
