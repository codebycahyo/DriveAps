<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Expose getExternalFilesDir(null) for sharing backup files -->
    <external-files-path name="backups" path="." />
    <!-- ExportHelper menulis PDF/CSV ke cacheDir; tanpa deklarasi ini
         FileProvider.getUriForFile melempar exception dan ekspor selalu gagal. -->
    <cache-path name="exports" path="." />
    <files-path name="files" path="." />
</paths>
